# Public R2 base URL — prefixed to every portfolio media path.
# This value is safe to commit (it's a public CDN URL) but lives in a
# per-environment file so dev / staging / prod can point at different buckets.
#
# Copy this file to `.env.local` before running `next dev` or `next build`.
NEXT_PUBLIC_MEDIA_BASE=https://pub-b6df9c86ce26430caf9d07b91b02796f.r2.dev
